# Install PyTorch FIRST, with pip inside the conda env (PyTorch no longer
# ships conda packages).
#
#   Apple Silicon / CPU:  pip install torch torchvision
#   NVIDIA:               pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# Then: pip install -r requirements.txt

numpy>=1.26
tqdm>=4.66

# Pinned to match requirements-pi.txt exactly. Quantization is version
# sensitive, so a PC that quantizes on one build and a Pi that measures on
# another produces numbers that cannot be compared -- including against the
# runs already recorded, every one of which used 1.27.0.
onnx==1.22.0
onnxruntime==1.27.0

# NSGA-II for the search stage. PC-side only -- the Pi measures candidates but
# never chooses them, so this stays out of requirements-pi.txt.
optuna>=3.6

# Testing. Scope is deliberately narrow: correctness-critical pure logic only
# (split guards, LR schedule, later config hashing and gate decisions).
# Measurement code is validated by parity checks against a reference, not by
# unit tests -- a mocked benchmark proves nothing about a real Pi.
pytest>=8.0
