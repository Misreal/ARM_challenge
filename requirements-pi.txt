# Raspberry Pi 5 benchmark agent dependencies. Deliberately minimal.
# Install on 64-bit Raspberry Pi OS (Bookworm) -- no aarch64 wheel on 32-bit.
#   python3 -m venv ~/armopt && source ~/armopt/bin/activate
#   pip install -r requirements-pi.txt
# NEVER install torch/torchvision here -- agent loads ONNX only.
# onnxruntime version MUST match the PC exactly all campaign; do not relax pins.
# onnx is required too: src.quant.measure/baselines/saturation_probe import onnx directly.

numpy>=1.26
onnx==1.22.0
onnxruntime==1.27.0
psutil>=5.9
