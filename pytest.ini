[pytest]
# `pythonpath = .` puts the repo root on sys.path so tests can `import src.*`
# without an editable install. Without it pytest inserts only `tests/`, and
# every import in the suite fails.
pythonpath = .
testpaths = tests
addopts = -q
